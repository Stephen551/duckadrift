node_modules/
dist/
*.log
.env
.specstory/

# Private build brief and session-law files. These stay dark permanently —
# never tracked, never public. See docs/adr/0001-governing-principle.md and
# docs/adr/0002-oracle-update-policy.md for what IS public about the process
# these files govern.
/CLAUDE.md
/PDR-duckadrift.md

# Local privacy-guard denylist — the actual forbidden names, never committed.
.privacy-denylist.local

# Private side of the calibration corpus (ADR-0040). Recordings, usage records,
# and review files captured from the two private internal logs carry their full
# decision-record text; they live only on the director's machine and are never
# tracked. The public side (calibration-corpus/public/) IS committed. This rule
# ships before the first private byte is captured, so the boundary is structural.
/calibration-corpus/private/
