# ADR-0004: Bold-line declared accepted

**Status:** Accepted

## Context

Status is a standalone bold title-block line — the dialect the semantic selector
matched alone before this primitive absorbed it. The S4 specimen uses this shape.

## Decision

Resolve to accepted from the bold-line source.
