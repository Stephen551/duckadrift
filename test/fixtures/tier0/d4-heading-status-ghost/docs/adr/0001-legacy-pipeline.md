# ADR-0001: Legacy pipeline

## Status

Superseded by ADR-0002

## Context

This record declares its status through a `## Status` heading section, the original ADR form's own canonical dialect, with no YAML frontmatter at all. It is the REFERENCE TARGET for this fixture: a source file outside the log still cites it, and its dead status is visible only when read from the heading dialect.

## Decision

N/A. Fixture file.

## Consequences

N/A. Fixture file.
