# Decision B

Found via the `<slug>/index.md` candidate — deliberately unreachable via any
correct relative-path depth from the linking ADR, so primary resolution
genuinely fails and this can only be found via the repo-wide fallback.
