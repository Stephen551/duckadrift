// Exists only to be linked from the ADR at a repo-root-relative path
// (src/real-file.ts), not resolvable ADR-dir-relative (docs/adr/src/real-file.ts
// doesn't exist) — proves the repo-root-relative fallback resolves it (ADR-0004).
export const seeded = true;
