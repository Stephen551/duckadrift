{
  "name": "duckadrift",
  "version": "0.1.9",
  "private": true,
  "description": "A GitHub Action that verifies an ADR log against reality.",
  "type": "module",
  "license": "MIT",
  "author": "Stephen551",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "duckadrift": "dist/cli/index.js"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "prepare": "node scripts/install-hooks.mjs || true"
  },
  "dependencies": {
    "mdast-util-from-markdown": "^2.0.3",
    "minimatch": "^10.2.5",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/mdast": "^4.0.4",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.9"
  }
}
